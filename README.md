# Get-Spotify-songs-names

```
When you select all (ctrl-a) in Spotify desktop app and copy (ctrl-c) the links of the songs will be the one copied.
```

```
To get the names just paste (ctrl-v) them in a text file names songsUrl.txt and install requirements.txt with pip (pip install -r requirements.txt)
```